#include <bits/stdc++.h>


//Write regex as \\n and not \n 
//remove whitesapces function
//
//

int jump_to_char(int size_arr, char meta_char[]){
  char last_char_to_match;
  char first_char;
  char second_char;

  for(int i = 0; i < size_arr; i++){
    if(meta_char[0] == '\\' && meta_char[1] == 'n'){
         
    }
    else if(meta_char[0] == '\\' && meta_char[1] == 't'){

    }
    else if(meta_char[0] == '\\' && meta_char[1] == 's'){

    }
    else if(meta_char[0] == '['){

    }
    else if(meta_char[0] == )
  }
}

//Should return vector of pairs with source and destination. Optimization may be done later. (or may be completely trivial
//Compilation time is trivial.  However, if you are interested in changing later.(lexing time may come into importance since
//this is shell))
std::vector<std::string> regex_scanner(std::string regular_exp){
  for(int i = 0; i < regular_exp.length(); i++){
    if()
  }
}


bool is_match(std::vector<std::vector <std::string> > dfa_table){

}
